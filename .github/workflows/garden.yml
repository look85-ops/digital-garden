name: garden grow

on:
  schedule:
    - cron: "0 */4 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  grow:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT }}
      
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install -r requirements.txt

      - name: Run curator
        env:
          EITHER_API_KEY: ${{ secrets.EITHER_API_KEY }}
          FREE_API_KEYS: ${{ secrets.FREE_API_KEYS }}
        run: python src/curator.py

      - name: Commit new artifacts
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git config user.name "garden-bot"
          git config user.email "garden-bot@users.noreply.github.com"
          git remote set-url origin https://look85-ops:${GH_PAT}@github.com/look85-ops/digital-garden.git
          git add artifacts/ index.html
          git diff --staged --quiet || git commit -m "garden: new artifact $(date -u +'%Y-%m-%d %H:%M')"
          git push